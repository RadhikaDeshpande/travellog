//////////////////////////////////////////////////////////////////////////
//                  Images API helpers
// Here we handle all requests related to the Images API END POINT 
// Usage: 
// var imagesApiHelper      = require(DEFS.DIR.API_HELPER_IMAGES);
// var imagesApiHelperObj   = new imagesApiHelper();
//////////////////////////////////////////////////////////////////////////

// Connect to the database
var dbConnect = require(DEFS.DIR.DB_CONNECT)('openshift');
var sp        = require(DEFS.DIR.DB_SP);

var images = function() {
  
}
module.exports = images;

